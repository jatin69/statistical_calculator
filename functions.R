my_mean<-function(x){
  # later on implement your own logic
  # result<-sprintf("%.6f",mean(x))
  # return(result)
  return(mean(x))
}


my_mode<-function(x){
  # later on implement your own logic
  result<-sprintf("%.6f",mean(x))
  return(result)
}


my_median<-function(x){
  # later on implement your own logic
  result<-sprintf("%.6f",mean(x))
  return(result)
}

